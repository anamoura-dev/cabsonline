# CabsOnline — copie para .env e preencha (não commitar .env)
#
# DB_CHARSET=utf8mb4 é recomendado em instalações novas; o código aceita utf8 por omissão.

DB_HOST=localhost
DB_NAME=bvf2703
DB_USER=root
DB_PASSWORD=
DB_CHARSET=utf8
